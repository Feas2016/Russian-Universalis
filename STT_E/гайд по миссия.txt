
<123> = {
    slot = <число>                 # Столбец миссий в игре. От 1 до 5.
    generic = <yes/no>             # Миссии в этой серии общие(generic) или нет.
    ai = <yes/no>                  # Будет ли искуственный интеллект выполнять миссии в этой серии.
    has_country_shield = <yes/no>  # Будет ли отображаться герб страны на иконке миссий    
    
    # Условия, при которых миссии будут загружены в игре. Используется для ограничений по ДЛС.
    potential_on_load = {
        <триггеры>
    }
    
    # Условия, при которых миссии будут у страны. Country scope, т.е. все условия (триггеры) проверяются применительно к стране
    potential = {
        <триггеры>
    }
    
    <1234> = {
        icon = mission_high_income         # Иконка миссии
        generic = <yes/no>     # Миссия относится к числу общих (generic)
        ai = <yes/no>          # Будет ли искуственный интеллект выполнять миссии в этой серии.
        position = <число>        # В каком ряду будет миссия. Единица - это самый первый ряд.
        completed_by = <дата>   # Автоматическое завершение миссии в истории 
        
        # Какие миссии нужно завершить, чтобы можно было выполнить эту
        required_missions = {
            <миссии>
        }
        
        # Подсветка необходимых провинций. Работает как all_province scope. Необязательно.
        provinces_to_highlight = {
            <триггеры>
		}
    
        # Условия, необходимые для завершения миссии. Country scope.
        trigger = {
            <триггеры>
			stability = 1  
        }
        
        # Эффекты, которые будут полученны по завершению. Country scope.
        effect = {
            <effect>
			add_country_modifier = {
				name = 123
				duration = 365
			}
        }
    }
}